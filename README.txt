MDT WEB (MTA:SA) - FIXMAP4 build

Change:
- Removed serial + discord from config/UI.
- Strong fallback mapping for id/name (fixes Unknown/#undefined even when DB rows returned).

Install:
1) stop old MDT resources
2) start mdt_web_dx_citizens_live_fixmap4
3) F6 -> Citizens
